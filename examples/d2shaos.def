BootStrap: debootstrap
OSVersion: stable
MirrorURL: http://ftp.us.debian.org/debian/

%runscript
    echo "This is what happens when you run the container..."

%post
    echo "Hello from inside the container"
    apt-get update
    apt-get -y install nano vim aptitude simple-cdd git tmux less debootstrap syslinux isolinux squashfs-tools genisoimage memtest86+ rsync
